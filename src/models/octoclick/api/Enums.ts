export enum PlacementType {
    WHITE_LIST = 1,
    BLACK_LIST
}
export enum CampaignStatus {
    ACTIVE = 1,
    PAUSED,
    ARCHIVED
}
export enum CreativeStatus {
    ARCHIVED = 1,
    BLOCKED,
    MODERATION,
    PAUSED,
    ACTIVE,
    DECLINED
}
export enum FilterType {
    ALLOW = 1,
    DISALLOW
}
export enum AdType {
    POPUNDER = 2
}